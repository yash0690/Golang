Was able to create using golang, but when serving it, it just loads the webpage as a text/html. Background and image are not represented.

Update: Actually webpage serves just fine except for the image not displaying.
